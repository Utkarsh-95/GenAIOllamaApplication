SpringAI Integration with Ollama GenAI application.

We can replace Ollama with OPENAI, for that we have to pass the key-value pair to env variables.

#spring.ai.openai.api-key=${OPENAI_API_KEY}
#spring.ai.openai.base-url=${BASE_URL}
